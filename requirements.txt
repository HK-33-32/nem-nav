# Human-readable shortcut. The source of truth for dependencies is pyproject.toml.
# To install everything used by CI, prefer:  pip install -e .[dev]
numpy>=1.24
faiss-cpu>=1.7
torch>=2.0
networkx>=3.0
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.65
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
psutil>=5.9
pytest>=7.0

# Optional extras (install via `pip install -e .[openclip]` etc.):
# open_clip_torch>=2.20      # OpenCLIP perception backend
# llama-cpp-python>=0.2      # local quantized LLM planner backend
# habitat-sim>=0.3           # Habitat backend (Linux + CUDA only)
# habitat-lab>=0.3
